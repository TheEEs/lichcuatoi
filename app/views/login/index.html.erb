<% if Rails.env.production? or Rails.env.test? %>
    <%= button_to "Đăng nhập với Facebook", "/auth/facebook/", method: :post, data: {turbo: false}%>
<% else %>
    <%= button_to "Đăng nhập", "/auth/developer/", method: :post, data: {turbo: false}%>
<% end %>